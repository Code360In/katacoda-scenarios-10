The correct answer is:

```
`<${tag}>Buy ${animal} food</${tag}>`
```{{execute}}

Try it out with different values:

```
tag = 'div'
animal = 'iguana'
`<${tag}>Buy ${animal} food</${tag}>`
```{{execute}}
